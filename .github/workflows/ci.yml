name: CI

on:
  pull_request:
  push:
    branches:
      - master

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies
        run: npm audit --audit-level=high

      - name: Lint, test, and build
        run: npm run check

      - name: Run crisis safety gate
        run: npm run crisis:gate

      - name: Install browsers
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Run Ladder acceptance cohort
        env:
          PLAYWRIGHT_PORT: "3221"
        run: npx playwright test e2e/ladder-persona-routing.spec.ts e2e/family-navigator.spec.ts --project=chromium --project=mobile --workers=1

      - name: Run Ladder cross-browser contract
        env:
          PLAYWRIGHT_PORT: "3222"
        run: npx playwright test e2e/ladder-browser-contract.spec.ts --project=firefox-smoke --project=webkit-smoke --project=mobile-webkit-smoke --workers=1

      - name: Run Ladder repeat-visit offline contract
        env:
          PLAYWRIGHT_PORT: "3223"
        run: npm run test:e2e:offline

      - name: Upload browser failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-results
          path: test-results
          if-no-files-found: ignore
