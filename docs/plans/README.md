# Patient Centered — Implementation Plans

This index is the authoritative lifecycle record for implementation plans. The plan bodies preserve their original reasoning and acceptance criteria; their lifecycle banners and the table below determine whether they are executable now.

Lifecycle meanings: **Active** is being implemented now; **Complete** landed its explicit sprint scope; **Rescoped** delivered a useful slice but requires a new bounded plan for any remainder; **Parked** cannot restart until its named gates are met; **Retired** must not be executed.

## Current lifecycle

| # | Plan | Status | Delivered / restart boundary |
|---|---|---|---|
| 00 | [Foundations](00-foundations.md) | **Rescoped** | F4 crisis routing landed in Sprint 08. Treat remaining portions of F1–F3 and F5–F10 as independent platform backlog items; restart one only when an approved consumer names its owner and acceptance gate. |
| 01 | [Early Detection](01-early-detection-plan.md) | **Parked** | No coherent implementation began. Restart only after demographics (F2), regulatory provenance (F5), and licensed dataset ownership (F10) are named. |
| 02 | [Between-Visit Monitoring](02-between-visit-monitoring-plan.md) | **Parked** | No coherent implementation began. Restart only after a heart-failure clinical owner, approved threshold source, and backend/worklist posture are settled. |
| 03 | [SDOH Resource Connection](03-sdoh-resource-connection-plan.md) | **Rescoped** | Sprint 08 delivered the deterministic material-needs screen and seeded resource finder. Closed-loop referrals, benefits workflows, and interoperability require separate backend-dependent plans. |
| 04 | [Behavioral Health](04-behavioral-health-plan.md) | **Rescoped** | Sprint 08 delivered crisis routing and minimal PHQ-9. Sprint 12 owns questionnaire generalization; longitudinal escalation and behavioral-care surfaces require a later clinical-workflow plan. |
| 05 | [Chronic Loops](05-chronic-loops-plan.md) | **Rescoped** | Diabetes work landed through the diabetes-first-class, provider-legible, and Tier-2 sprints. Deprescribing, cost transparency, and COPD must return as separate clinically owned and licensed plans. |
| 06 | [Care Coordination](06-care-coordination-plan.md) | **Parked** | No clinician/multi-patient implementation should begin before backend authorization, actor-bearing audit, and regulatory-copy gates land. |
| 07 | [Access Equity](07-access-equity-plan.md) | **Rescoped** | Sprint 08 delivered the core voice-safety and accessibility slice. Broader multilingual and offline work must return as discrete, release-gated plans. |
| 08 | [rhtp Integration](08-rhtp-integration.md) | **Complete** | P0–P8 landed on `master` on 2026-07-06. |
| 09 | [DR Pathway Fold-In](09-dr-pathway-fold-in-and-retire.md) | **Complete** | P0–P8 landed and the prototype was archived and retired on 2026-07-07. |
| 10 | [Diabetes Provider-Legible Loop](10-diabetes-provider-legible-loop.md) | **Complete** | P0–P3 landed on `master` on 2026-07-07. |
| 11 | [Diabetes Loop Tier 2](11-diabetes-loop-tier2.md) | **Complete** | P0–P4 landed on `master` on 2026-07-07. |
| 12 | [Screening Hub](12-screening-hub.md) | **Active** | Implementation began from `eccdcd8` on 2026-07-20. Do not fold parked behavioral or SDOH scope into it beyond its locked questionnaire-engine plan. |
| 13 | [Family Navigator II — Rank & Justify](13-family-navigator-rank-justify.md) | **Active** | Implementation began from `0b034c5` on 2026-07-21 (spec 11). P0–P5: harm-to-others crisis domain, family safety banner (acknowledge-and-continue), procedural catalog entries, vignette gate, `/api/family/recommend` + mock ranker, ranked UI. Shares the tree with plan 12 — keep commits path-scoped and do not touch screening seams. |
| 14 | [UKHCI Ladder — Rebrand + Evaluation-Visit Loop](14-ukhci-ladder.md) | **Complete** | Tasks 1–6 landed on `master` on 2026-07-24 (`73164e5`→`9e6364c`) with two hardening follow-ups (`38e5f6b` state/demo-UX coherence, `5d1329b` impossible-reminder-history rejection). Post-build verification 2026-07-24: `check`, `crisis:gate`, and the family e2e suite (23 passed, incl. the Ladder journey on chromium + mobile) all green; live smoke on `/ladder` confirmed redirect, booking, barrier→resource routing, reminder ladder, and audit trail. Shipped to production 2026-07-24 (sha `4dd2bb4`, deploy `dpl_H8ReZXsxssdWBx8MU7uYHBcrWAxT`, https://patient-centered.vercel.app/ladder); prod-verified 200s plus the `/family`→`/ladder` 308 with query preserved. |
| — | [Adherence & Trust Uplift](../adherence-trust-uplift-execution-plan.md) | **Retired** | The 55-task umbrella remains non-executable. Its three bounded replacements landed on `master` on 2026-07-20: adherence/barrier UX, opt-in reminders/PWA, and runtime-mode privacy disclosure. |

The runtime-mode disclosure copy has [recorded product-owner approval](../ops/ai-data-disclosure-approval.md). Independent legal, privacy-officer, clinical, and regulatory approval remains required before real-patient or production exposure.

## Retired branches

- `claude/agitated-tharp-cccd48` — retired 2026-07-20 at `078ca302596759b804f1dee4958fe18179ac9f7d`. This unmerged July 7 voice-safety experiment was superseded by Sprint 08 and later `master` work; both the local and remote branches were deleted after the tip was recorded here.

## Historical planning detail

The table below is preserved as the pre-execution decision record for plans 01–07. It is not a current queue; the lifecycle table above controls.

| # | Plan | Spec | Decisions needed before build |
|---|------|------|-------------------------------|
| 01 | [Silent-Killer Early Detection & Risk Stratification — Implementation Plan (P0)](01-early-detection-plan.md) | [spec](../specs/01-early-detection.md) | FDA non-device-CDS sign-off for the patient-facing ADA score: does P0 real-patient exposure wait on the full F5 `regulatory-posture-design-spec.md` counsel read, or can P0 ship to real patients under a documented interim classification (with local/fixture demo proceeding regardless)? This is the gate that decides whether P0 is demo-only or patient-facing.; Persona/fixture decision: confirm P0 introduces a *new* scorable `doloresState` fixture (age ~54, female, family-history diabetes, BMI ~31, not-yet-diabetic). The existing fixtures don't fit — Jordan has no glucose/demographics and Brent is already diabetic (A1c 8.0%, metformin), so neither can demo *undiagnosed*-prediabetes detection.; Named clinical + legal owner for the F10 `ada_cdc_diabetes` registry entry (points table, USPSTF cadence, version/effectiveDate) and for the prevalence figures cited in the spec's Problem section — required before patient exposure since a stale rule is a safety issue. Who signs off and on what cadence?; Accept self-reported/proxy metrics for all of P0 (screening-completion and care-team rejection-rate are unmeasurable without F8's return channel), or pull F8 partially forward? This determines whether the primary clinical outcome is even observable in P0. |
| 02 | [Triaged Between-Visit Remote Monitoring — HF & Post-Discharge (Implementation Plan)](02-between-visit-monitoring-plan.md) | [spec](../specs/02-between-visit-monitoring.md) | Safety-gate ordering + gating: do you accept the requirement that the HF escalation branch is hard-gated on `carePlan.condition === "heart_failure"` AND a present HF signal (SymptomCheckIn or crossed weight rule)? This is the only thing that keeps every existing `safety-gate.test.ts` assertion green while inserting the HF branch above stored-vitals. If instead you want HF logic to run for any patient, the existing branch-order tests will change and this becomes a much larger, riskier safety refactor.; Coupling to `task_e569880c`: the draft treated the whole safety-gate task as blocked on the crisis-pathway merge. Verified reality is that HF red-flags are NOT crisis, so only P0-7's final wiring (exporting `CARE_TEAM_ACTIONS` — currently a module-local const — and placing the HF branch below `crisis_escalate`) coordinates with it. Do you want P0-7 to land against today's gate now (exporting the constant itself) and reorder in a one-liner when crisis merges, or hold P0-7 until the merge? The deterministic core (P0-5/P0-6) proceeds regardless.; Regulatory copy line (F5): must the P0 patient-facing suggested action stay as directive-ish 'same-day diuretic review', or be softened to 'consider contacting your clinic' pending counsel's non-device-CDS read? This blocks the first suggested-action string and the worklist card copy.; Worklist isolation posture: `/worklist` is a clinician surface running inside the same single-user, no-auth app and reading the same localStorage key as the patient. Confirm it ships fixture-only as a demo with an explicit in-product 'not for real PHI' boundary until F1 + F3 land — or whether even a fixture clinician view in the shared app is acceptable for your demo audience. |
| 03 | [Implementation Plan — Social Determinants Screening & Resource Connection (Food-is-Medicine)](03-sdoh-resource-connection-plan.md) | [spec](../specs/03-sdoh-resource-connection.md) | Escalation tiering: ship P0 now with the material emergency as a self-contained hard_escalate and re-tier it under crisis_escalate later (P0-5b, after task_e569880c merges), or hold the gate work until the crisis session lands? The draft's 'insert below crisis_escalate' instruction is un-actionable today because that branch does not exist in src/ (grep-confirmed) — a decision is required, not an assumption.; HealthBrief per-item EvidenceStatus: the shared HealthBrief.sections type carries one status per section, not per item, so FR-12's 'per-item EvidenceStatus' cannot be taken literally. Choose: emit one social section per distinct status (recommended), or encode status inline in item strings with a section-level conservative status. Do not change the shared HealthBrief type for P0.; FR-11 truncation strategy: buildTodayTasks slices to MAX_TODAY_TASKS=3 (tasks.ts:102), so a low-priority social task is silently dropped. Confirm the split — non-urgent social follow-ups live on /social, and only priority-1 urgent social tasks are force-injected into today ahead of the slice.; Component home: there is no src/components/ directory today (UI lives under src/app/*). Decide before P0-14 whether the three social components colocate under src/app/social/ or a new src/components/ tree is introduced, so the plan's file paths are real. |
| 04 | [Integrated Behavioral Health & Crisis Escalation — Implementation Plan](04-behavioral-health-plan.md) | [spec](../specs/04-behavioral-health.md) | Crisis-half sequencing: the entire P0-CRISIS milestone hard-blocks on the still-unmerged task_e569880c (verified: no crisis/988 token exists in src/ yet). Spec 01 sidestepped this by deferring its PHQ path to P1, but crisis IS the feature here. Do you accept that P0 cannot fully ship until task_e569880c merges, or should that task be pulled forward / co-owned to unblock this sprint?; Merge gate vs. release gate for the crisis Playwright e2e: `npm run check` is lint+Vitest+build only — Playwright is the separate `test:e2e` script and does NOT block merge today. The plan makes every crisis guarantee a Vitest test and treats the e2e as a CI release gate. Is that acceptable, or do you want the crisis e2e wired into the merge-blocking gate (add it to `check` or a required CI job)?; Where does the `crisis_escalated` audit get written — at the gate decision seam (guaranteed to fire whenever decideSafety returns crisis, but couples the pure gate to the audit trail) or at the UI call site (decoupled but skippable, risking a missing legal record)? The plan chose the seam; confirm, or specify a UI-integration test that fails if the audit is ever absent.; AuditEvent['action'] union collision with F3: this plan adds three action strings (assessment_recorded/escalation_raised/crisis_escalated) to the same closed union F3 is centrally extending (plus F3 adds an actor field). Do you land these three in P0 now and reconcile names at F3 merge (P0 audits stay anonymous/system-actor), or wait for F3 before writing any behavioral audit code? |
| 05 | [Implementation Plan — Chronic-Disease Self-Management Loops + Deprescribing & Cost Transparency](05-chronic-loops-plan.md) | [spec](../specs/05-chronic-loops.md) | Storage-validation strategy for the new arrays: validate deprescribeCandidates/costFlags in the STRICT core path (a single malformed entry discards the whole persisted state) or move them into the loose PersistedAppState/sanitize path (drop only bad entries)? The draft glossed this as 'add a backfill line', but the real storage.ts architecture (PersistedAppState = Omit<..., tasks\|mealLog\|doseEvents>, with only those three sanitized) forces a per-array decision before P0-3. Recommend strict for these small internal arrays, sanitized for the P1 patient-entered glucose/activity streams.; Deprescribing ruleset ownership, licensing, and update cadence: who is the named clinical owner, is a licensed Beers/STOPP + interaction dataset (with its terms) required before P0's seeded table can reach any patient, and does F5 legal/clinical sign-off gate P0 exposure or only the P1 live swap? This is the true long-lead item and a duty-to-act liability, not a code task.; Should P0's AppState add all four event arrays (deprescribeCandidates, costFlags, glucoseReadings, activityEvents) in one migration, or only the two P0 actually reads (deprescribe/cost), deferring glucose/activity to P1? Adding inert P1 arrays in P0 means dead state that every guard, fixture, export, and delete path must carry with zero P0 behavior — a scope-discipline call.; Confirm task_e569880c's as-built shape before the P1 hypo/hyper slice starts: does it EXPORT the care-team/crisis action constants (CARE_TEAM_ACTIONS is currently module-private at safety-gate.ts:7), what is the crisis action string and CRISIS_ACTIONS name, and is the gate re-ordered so crisis is the literal first branch with a green ordering test? P1-2 is hard-blocked on all of these and must not hard-code ['call_clinic','draft_message']. |
| 06 | [Care-Coordination Quarterback — Transitions & Referral Loops (Implementation Plan)](06-care-coordination-plan.md) | [spec](../specs/06-care-coordination.md) | SaMD boundary sign-off: who (counsel + clinical lead) reviews every reconciliation and transition-summary string against current FDA CDS guidance, and is that review a hard P1 gate? The moment copy reads 'these interact — hold X' or 'recommend dose Y' it becomes a regulated device — this is the single highest-liability decision and it blocks P1.; Heart-failure modeling ownership and scope: HF is unmodeled today, and its weight-trend monitoring does NOT fit the food-shaped `ConditionLens` type (it needs a separate monitoring/threshold layer). Is the HF condition + lens + monitoring layer inside this feature's P1, or a shared F6 platform item? Until it lands, HF discharges get the generic fallback and no 'HF lens' may be demoed.; ADT/HIE feed partner for P1: regional HIE vs. one pilot facility's FHIR `Encounter`/`MedicationRequest` webhooks? This is the biggest external dependency and the P1 critical path — nothing in the clinician queue is real without it, and it requires a BAA plus a no-retention LLM tier before any PHI flows.; Backend ownership (F1): confirm 'the app has a server' is tracked as a shared platform prerequisite owned outside this feature (this feature is the first consumer, not the owner), so P1 isn't blocked re-litigating it — and confirm whether the tiny F3 (audit actor) and F9 (i18n multi-catalog) slices land centrally or are absorbed into this feature's P0-0/P0-0b. |
| 07 | [Reaching the Unreached — Voice-First, Multilingual Access (P0)](07-access-equity-plan.md) | [spec](../specs/07-access-equity.md) | FR-6a live-voice gate: which mechanism ships first — (i) classify the user transcript before response.create and suppress on hard-escalate, or (ii) route escalation-capable turns through the server createSafeAiResponse path instead of direct browser↔OpenAI streaming? This is an engineering-and-release decision needing clinical sign-off; live WebRTC Coach cannot release without it.; Escalation-tier product promise: the current gate routes a stored high reading that meets the care-plan callThreshold to soft_escalate (not hard-escalate), so 'SBP≥180 is always emergency framing' is not universally true. Does product/clinical want to (a) accept the gate's existing tiering and phrase copy accordingly, or (b) change the gate so any dangerous vital hard-escalates regardless of threshold — a change owned with F4/task_e569880c, not this feature?; task_e569880c shape: what are the as-built crisis action constant name (call_988 vs call_911), the CRISIS_ACTIONS export, and the final branch order? Voice work and the parity test must consume the as-built shape; someone must confirm the merge and update F4 before P0-C/D starts.; Safety-critical translation boundary: clinical + legal must sign off exactly which string keys require human review (P0-B2 enumerates keys, but the clinical boundary and the emergency-number-per-region policy are process decisions, not engineering judgment), and whether US-locale hard escalations should name 911 explicitly. |

_Each plan follows a fixed structure: P0 Definition of Done, prerequisites, architecture, a granular sequenced work breakdown, data-model/storage changes, AI wiring, testing strategy, rollout, risks, and effort sizing. P0 in every plan is scoped to run on the current localStorage prototype; anything needing the backend is pushed to a later milestone._
