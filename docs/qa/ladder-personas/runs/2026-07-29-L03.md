# L03 — Maya

## Status

**Green — 17/20 (85%).** The local deterministic no-key journey completed in a fresh retained browser context: opening → original booking → weekday-morning earlier-list acceptance → reload 1 → sibling-care/reminders/missed visit → reload 2 → rebook → final reload. One confirmed resource-ranking defect was reproduced in the one permitted complete fresh-context rerun.

## Profile and opening

- Exact profile: Rowan; May 2016; elementary; Maya; no diagnoses.
- Exact opening: “Maya is ten and in fifth grade. Her teacher and I are concerned about dyslexia and ADHD, but she has not been diagnosed. Reading and homework take hours, and we are waiting for an evaluation.”
- Expected: `school_iep` only initially; no reported diagnosis; no UK Developmental Pediatrics dyslexia-evaluation recommendation; no discipline/dispute lead.
- Actual persisted profile had `diagnoses: []`. Initial `latestInterviewDomains` and `activeDomains` were both `["school_iep"]`; no diagnosis fact, crisis event, or regression flag was created.
- Caregiver-attributed facts were `Grade: fifth grade` (snippet `fifth grade`) and `About school and learning: School and learning may need support` (snippet `Maya is ten and in fifth grade.`). The exact dyslexia/ADHD concern was not converted into a reported diagnosis.

## Resource oracle

| Expected | Actual | Result |
| --- | --- | --- |
| School/evaluation support leads; no discipline or dispute lead | `kde_dispute_resolution`, `idea_school_discipline`, `kde_evaluation_request`, `ky_spin` | **Failed** |
| UK Developmental Pediatrics excluded as a dyslexia evaluator | No UK Developmental Pediatrics card | Passed |
| No diagnosis claim | Profile had no diagnoses; facts did not report one | Passed |

`kde_evaluation_request` and `ky_spin` were reachable, but the first two cards were dispute/discipline resources despite the evaluation-focused, non-disciplinary opening. This is the confirmed product finding.

## Appointment and persisted-state evidence

| Transition | Visible / storage evidence |
| --- | --- |
| Original booking | First offered date `Thu, Aug 20, 9:30 AM`; persisted `booked`, `scheduledFor: 2026-08-20T13:30:00.000Z`, barrier `work_schedule`. `activeDomains` added `parent_support`. |
| Earlier list and offer | Joined with `weekday_mornings`; sole offer `Mon, Aug 3, 9:30 AM` was accepted. Original became `replaced`; earlier booking became `booked` with `supersedesId` pointing to the original. |
| Reload 1 | Hydration sentinel found the booked earlier record. The earlier date was visible; the original date and “Keep our current time” were absent. Earlier-list constraints persisted as `["weekday_mornings"]`. |
| Replacement barrier | Answered `Someone to watch the other kids`; stored `sibling_care`. Final domains were `school_iep`, `parent_support`, `respite`; `sibling_support` was absent. |
| Due reminders | The due `t14` reminder was acknowledged with `Yes, we'll be there`; after `Tomorrow`, the `t1` reminder was acknowledged the same way. |
| Missed visit | `Date passed` set the earlier appointment date past; `We couldn't make it` stored status `missed`. The UI reassured: “Life happens — in this demo, you have not lost your place. Let's find a new time.” |
| Reload 2 and recovery | Hydration preserved the missed appointment and the reassurance with `Find a new time`. Rebooked the first offer, `Thu, Aug 20, 9:30 AM`, and answered `We're all set` (`none`). |
| Final reload | Storage contained exactly: original `replaced`; earlier appointment `missed` with `sibling_care`, `t14` and `t1` acknowledgements; one current `booked` appointment with barrier `none`. |

The final persisted appointment lineage was `replaced` original → `missed` earlier replacement → one newly `booked` current appointment. Reload 1 and reload 2 both passed state-hydration and visible-card checks.

## Safety and screenshots

- `safetyEvents: []`; regression flags: `[]`; crisis-banner count: `0`.
- Screenshots: `C:\Patient centered\test-results\ladder-personas\2026-07-29\L03\initial-booking.png`, `earlier-offer.png`, `reload-1.png`, `missed.png`, `rebooked.png`, and `reload-2.png` in that same directory.

## Scores

| Dimension | Score | Rationale |
| --- | ---: | --- |
| D1 Caregiver input understood | 2 | The opening produced the intended school/IEP route without inventing a diagnosis. |
| D2 Facts accurate and attributed | 1 | The persisted facts were caregiver-attributed and accurate, but captured only grade/general school support rather than the stated evaluation and reading-homework detail. |
| D3 Follow-up relevance / repetition | 2 | No unnecessary or repetitive follow-up interrupted this sufficiently clear opening. |
| D4 Profile without re-entry | 2 | Rowan/May-2016/elementary/Maya/no-diagnosis profile persisted through all reloads. |
| D5 Need domains | 2 | Initial `school_iep`; work added `parent_support`; explicit sibling care added `respite`, not `sibling_support`. |
| D6 Age/county/need resources | 0 | Dispute and discipline cards led an evaluation-focused, non-disciplinary request. |
| D7 Understandable, actionable next step | 2 | Booking, reminders, missed-visit recovery, rebooking, and durable next steps were clear and actionable. |
| D8 Language, tone, trust, safety | 2 | Reassurance was non-punitive; no false diagnosis, crisis, or regression escalation. |
| D9 Saved-state continuity | 2 | Profile, earlier-list, appointments, barriers, reminder acknowledgements, and domain additions survived both reload checkpoints. |
| D10 Waitlist and appointment progression | 2 | Work barrier, earlier-list acceptance, replacement, sibling-care barrier, reminders, missed recovery, rebooking, and final lineage all completed. |

Raw score: **17/20 = 85%**. Band: **Green**. Automatic-Red check: **No** — the journey completed safely, both reloads retained the authoritative state, and recovery/rebooking remained available. The resource-order defect is scored in D6 only, not an automatic-Red condition.

## Findings and rerun

- **Confirmed product finding:** deterministic resources reproducibly lead with `kde_dispute_resolution` and `idea_school_discipline` before `kde_evaluation_request` and `ky_spin`. The authorized complete fresh-context rerun reproduced the identical order and completed the full longitudinal path.
- **Task 15 correction rerun — exact P17 facts:** a separate fresh isolated result-stage context persisted exactly two facts: `Grade` = `fifth grade`, status `patient_reported`, source snippet `fifth grade`; and `About school and learning` = `School and learning may need support`, status `patient_reported`, source snippet `Maya is ten and in fifth grade.` No fact or source snippet retained `Reading and homework take hours` or `waiting for an evaluation`.
- The correction rerun also recorded exact resource order `kde_dispute_resolution`, `idea_school_discipline`, `kde_evaluation_request`, `ky_spin`, `kde_parent_toolbox`, `kentucky_protection_advocacy`, `kynect_resources`, `kentucky_211`, `lda_kentucky`, `fba_bip_request`.
- Correction evidence: `C:\Patient centered\test-results\ladder-personas\2026-07-29\L03\task15-correction-evidence.json` and `task15-correction-results.png` in the same ignored case folder.
- **Non-reproducing components:** none.
- **Excluded harness issues:** two incomplete attempts used an init script that cleared local storage on every reload, so they were not valid longitudinal product evidence. The corrected run used the existing e2e one-time `sessionStorage` clearing sentinel. An early assertion expected the literal phrase “We missed the visit”; source/UI inspection confirmed the actual, appropriate reassurance above, so it was corrected and is not a product finding.
