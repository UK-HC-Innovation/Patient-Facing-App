# Ladder persona stress-test local baseline — 2026-07-29

## Checkout

- Branch: `master`
- Starting commit: `83c5415 docs(qa): clarify authoritative Ladder path`
- `git status --short`: no output before this task (clean checkout).
- No project-root `AGENTS.md` was present at `C:\Patient centered\AGENTS.md`.

## Deterministic gates

| Command | Exit | Duration | Result |
| --- | ---: | ---: | --- |
| `npm run check` | 0 | 28.600 s | PASS — lint, Vitest, and production build completed. |
| `npm run crisis:gate` | 0 | 27.874 s | PASS — six files and 310 tests passed. |
| `npm run test:e2e -- e2e/family-navigator.spec.ts` | 0 | 27.500 s | PASS — existing family-navigator desktop and mobile coverage completed. |

No focused rerun was needed. `npm run navigator:gate` was deliberately not run: it requires a live API key and is outside this deterministic no-key baseline.

## Local app and empty-start checkpoint

- Authoritative URL: `http://127.0.0.1:3000/ladder`
- Server: `npm run dev -- --hostname 127.0.0.1 --port 3000`; listening on `127.0.0.1:3000` as PID `40072`.
- Process identification: `Get-NetTCPConnection -LocalAddress 127.0.0.1 -LocalPort 3000 -State Listen` (then use `OwningProcess`).
- Uncommitted log: `C:\Patient centered\.superpowers\sdd\2026-07-29-ladder-persona-stress-test\task-2-dev-server.log`.
- Browser: Chrome extension engine, fresh agent-created tab, viewport `1536 × 791` CSS pixels, DPR `2.5`.
- Visible UI checks: PASS — heading `Ladder — your child's development`; `UKHCI Ladder · concept demo — not an official service`; opening interview `Tell us about your child and their needs`; blank response field; collapsed `Add or change your child's details` prompt (no seeded family profile); no crisis/banner text or alert.

## Crisis report

`npm run crisis:gate` rewrote `docs/ops/red-team-results/2026-07-29-crisis-gate.md`. The revision-context block was removed and execution timestamps, ordering, and durations changed, so the changed report is included with this baseline commit.

## Scope

Only this baseline record and the changed crisis-gate report are intended for commit. No application source, tests, resource catalogs, or product behavior changed.
